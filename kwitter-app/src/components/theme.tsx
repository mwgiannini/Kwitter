import { createTheme } from '@mui/material/styles';

export const MyTheme = createTheme({
  palette: {
    primary: {
      main: '#f9590d',
    },
    secondary: {
      main: '#ff5a18',
    },
  },
});