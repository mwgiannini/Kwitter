import { createTheme } from '@mui/material/styles';

export const MyTheme = createTheme({
  palette: {
    primary: {
      main: '#ffcd18',
    },
    secondary: {
      main: '#ff5a18',
    },
  },
});